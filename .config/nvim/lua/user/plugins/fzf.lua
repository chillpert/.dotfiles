local actions = require "fzf-lua.actions"
require "fzf-lua".setup {
    winopts = {
        fullscreen = true,
        preview = {
            vertical = "down:40%",
            layout = "vertical",
        },
    },
}
