# Dotfiles - Windows

Set `XDG_CONFIG_HOME` environment variable to `C:\Users\chillpert\.config`, so that `nvim` and other programs can find their configuration files.

Run `winget source remove msstore` to remove the Microsoft Store as a source for `winget`.
